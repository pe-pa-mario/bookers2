<% if @current_user.errors.any? %>
  <%= @current_user.errors.count %>errors prohibited this book from being saved:

  <ul>
    <% @current_user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-sm-5 mx-auto">
　　　        <h2>User info</h2>

　　        　<%= form_with model: @user do |f| %>
               <div class="form_group">
  　         　<label for="input Name">Name</label>
  　         　<%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"Name"%>
  　         　</div>

               <div class="form_group">
  　         　<label for="inputImage">ProfileImage</label>
  　          　<%= f.file_field :profile_image, placeholder: "ProfileImage", accept: "image/*" %>
  　          　</div>

               <div class="form_group">
  　           <label for="inputIntroduction">Introduction</label>
  　          <%= f.text_field :introduction, autofocus: true, id:"inputIntroduction", placeholder:"Introduction"%>
  　          </div>

             <div class="form_group">
             <%= f.submit "Update User" %>

             </div>
              <% end %>
           </form>

             <div class="cotainer mt-5">
                 <div class="row">
                       <div class="mx-auto">
                     <p2>CopyRight Infratop.inc</p2>
                      </div>
                </div>
              </div>
        </div>
      </div>
</div>
